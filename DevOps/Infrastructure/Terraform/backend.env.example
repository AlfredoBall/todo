# Example environment variables for Terraform backend initialization
# Copy this file, replace placeholders with your actual values, and source it.
# DO NOT commit the file with real values to source control.

# Azure resource group containing the Terraform state storage account
TF_RESOURCE_GROUP=<your-tfstate-resource-group>

# Storage account name (must be globally unique, 3-24 lowercase alphanumeric characters)
TF_STORAGE_ACCOUNT=<your-storage-account-name>

# Container name within the storage account
TF_CONTAINER=tfstate

# State file name/key
TF_KEY=todo.terraform.tfstate

# Azure subscription ID (GUID format)
TF_SUBSCRIPTION_ID=<your-subscription-id>

# Azure tenant ID (GUID format)
TF_TENANT_ID=<your-tenant-id>

# Example usage:
#  # PowerShell
#  $env:TF_RESOURCE_GROUP = 'todo-rg'
#  ...
#  .\DevOps\Scripts\terraform-init.ps1
#
#  # Bash (source the file then run script)
#  source DevOps/Infrastructure/Terraform/backend.env.example
#  DevOps/Scripts/terraform-init.sh
